# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook
TELEGRAM_WEBHOOK_SECRET=your_secret_here

# Google OAuth2 (скачать credentials.json отдельно)
GOOGLE_CREDENTIALS_PATH=credentials.json

# LLM Provider (OpenAI-compatible: Grok, OpenAI, etc.)
LLM_API_KEY=your_llm_api_key
LLM_BASE_URL=https://api.x.ai/v1
LLM_MODEL=grok-3
LLM_TEMPERATURE=0.7
LLM_MAX_RETRIES=3
LLM_TIMEOUT=30.0

# Redis (conversation history + OAuth2 tokens)
REDIS_URL=redis://localhost:6379/0
CONVERSATION_TTL_SECONDS=86400
TOKEN_TTL_SECONDS=2592000

# PostgreSQL + pgvector (future RAG)
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/assistant
